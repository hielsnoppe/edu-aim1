'use strict';

/**
 * Module dependencies.
 */
 

var app = require('./config/lib/app');
var server = app.start();
require('request');
 
var org2='Alexanderplatz';
var org='52.501590,13.342805';
var origin='';
if(org2){
origin=org2;
}else{
origin=org
}
var dest='Leopoldplatz';
var place=',+NY';
var postcode=',+NJ+07073';
var request = require('request');
var obj=request('https://maps.googleapis.com/maps/api/directions/json?origin='+origin+'&destination='+dest+'&mode=transit&alternative=true&key=AIzaSyBCroIiU9zWXaFxW0SE62fcSGxdQsP0XiY', function (error, response, body) {
  if (!error && response.statusCode == 200) {
    console.log(body); // Show the HTML for the Google homepage.
	
	
  }
});

