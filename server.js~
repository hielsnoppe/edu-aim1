'use strict';

/**
 * Module dependencies.
 */
var app = require('./config/lib/app');
var server = app.start();
require('request');

request('https://bvg-api.herokuapp.com/station?input=alexanderplatz', body)
  
 log.console(body);
  
;
